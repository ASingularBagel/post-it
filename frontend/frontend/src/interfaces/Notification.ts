export default interface Notification {
    createdAt: String; 
    imageUrl: String;
    postId: String;
    notificationId: String; 
    read: String; 
    recipient: String;
    sender: String; 
    type: String;
}